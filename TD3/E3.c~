
#include<stdio.h>


/**
* \file E3.c
* \brief Programme de calcule de la distance entre deux points.
* \author http://axiomcate.fr
* \version 1.0
* \date octobre 2015

* \fn float calculerDistance(Point point1,Point point2)
* \brief calcule la distance entre deux points
* \param point1 Point1 pour le calcule de distance 
* \param point2 Point2 pour le calcule de distance 
* \return Un float représentant la distance calculée
 


* \enum color
* \brief Couleur possible pour un point.
* La table de correspondance de couleurs est disponible en modification par les acesseurs.



*/

typedef enum{COLOR_BLUE,
COLOR_DARK_RED,
COLOR_OLIVE}color;

float calculerDistance(Point point1,Point point2){
 Point point1;
 Point point2;
/** La distance entre les point1 et point2 est calculée par l'intermédiare des coordonnéées des points*/
}
